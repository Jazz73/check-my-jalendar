# Copy to .env for local `vercel dev`, and set the same two in Vercel project settings.
# Supabase -> Project Settings -> API
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# Use the SERVICE ROLE key (server-side only — never commit or expose to the browser).
SUPABASE_SERVICE_KEY=your-service-role-key
