{
  "name": "check-my-jalendar",
  "version": "1.0.0",
  "private": true,
  "description": "A simple when-to-meet group scheduler. Name-only login, shareable event URLs, 30-minute availability chunks.",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "nanoid": "^5.0.7"
  }
}
